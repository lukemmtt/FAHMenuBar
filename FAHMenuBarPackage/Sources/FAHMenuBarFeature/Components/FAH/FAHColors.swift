import SwiftUI

struct FAHColors {
    static let running = Color(hex: "008001")     // Green - running/folding state
    static let paused = Color(hex: "FFA500")      // Yellow - paused state  
    static let finishing = Color(hex: "00CC99")   // Blue - finishing state
}